# {spot} Monitoring UI

Historical note:
- the dedicated browser monitoring pages and eval monitor endpoints described here are no longer part of the active delivery baseline
- the supported operator surface is now the native macOS app under [`app/macos/`](/Users/moldovancsaba/Projects/spot/app/macos)

Current workspace implementation baseline: `0.5.0`

## Current Monitoring Surface

Supported monitoring and process control now live in:
- the native macOS workspace
- the loopback runtime health endpoint: `GET /api/health`
- the JSON runtime/run/review/artifact endpoints consumed by the native app

## Historical Scope

This document is retained only to explain older browser-era monitor references that may still appear in historical handover logs and release notes.
